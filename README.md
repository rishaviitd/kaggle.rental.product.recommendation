# kaggle.rental.product.recommendation
The task was to build a model that, given the metadata of a browsing session and the sequence of visited pages, predicts which product pages the user will visit next.
